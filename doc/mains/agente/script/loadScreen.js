window.addEventListener("load", function(){ //JavaScript cuenta con una herramienta para facilitar la transicion y la desaparicion de la animacion
    document.getElementById("loader").classList.toggle("loader2") //Se inidca que el primer elemento llamada "loader" sea intercambiado por el elemento "loader2"
})
